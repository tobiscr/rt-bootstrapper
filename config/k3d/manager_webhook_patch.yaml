apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
  labels:
    app.kubernetes.io/name: rt-bootstrapper
    app.kubernetes.io/managed-by: kustomize
spec:
  template:
    spec:
      containers:
      - name: manager
        ports:
        - containerPort: 9443
          name: webhook-server
          protocol: TCP
        volumeMounts:
          - mountPath: "/tmp"
            name: rt-bootstrapper-certificates
            readOnly: true
      volumes:
      - name: rt-bootstrapper-certificates
        secret:
          secretName: rt-bootstrapper-certificates
