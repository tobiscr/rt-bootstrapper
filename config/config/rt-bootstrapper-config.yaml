apiVersion: v1
data:
  rt-bootstrapper-config.json: |
    {
      "imagePullSecretName": "registry-credentials",
      "overrides": {
        "replace.me": "ghcr.io"
      }
    }
kind: ConfigMap
metadata:
  name: config
  namespace: system
